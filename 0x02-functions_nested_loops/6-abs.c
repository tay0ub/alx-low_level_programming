#include "main.h"

/*
 * task 6
 */

/**
 * _abs - calcule absulute value of an int
 * @n: int witch the absulute value will be calculated
 * Return: absulute value
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
